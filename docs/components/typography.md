# Typography

Typography scale documentation (covered by Text).

**Category:** Basic · **Status:** Planned (Wave 3) · **Element Plus reference:** https://element-plus.org/en-US/component/typography

This component is on the Cobos UI roadmap and is not yet implemented. The API will mirror the Element Plus component linked above, adapted to React (props 1:1 where sensible; `v-model` → `value`/`onChange` + `defaultValue`; slots → `children`/render props; emits → `on*` callbacks).
